import type { FC } from "react";
import LoginForm from "../../components/LoginForm/LoginForm";

const Home: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Home;
